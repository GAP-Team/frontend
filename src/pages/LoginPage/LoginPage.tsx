import React from "react";
import styles from "./LoginPage.module.scss";
import cx from "classnames";

export default function LoginPage() {
  return (
    <div className={cx(styles.loginPageContainer)}>
      <h1>LoginPage</h1>
    </div>
  );
}
