import { SxProps } from "@mui/system";

export const styles = {
  button: {
    borderRadius: "0.5rem",
    margin: "0.5rem",
    py: "0.5rem",
    fontWeight: 600,
    textTransform: "capitalize",
  },
} as const;
