version: '3.7'

services:
  gap_frontend:
    container_name: gap_frontend
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./fe/:/usr/src/app
    networks:
      - gap_network

networks:
  gap_network:
